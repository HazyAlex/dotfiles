---
- name: Base Server Configuration
  hosts: all
  become: yes
  vars_files:
    - ../vars/base_packages.yml
  tasks:
    - name: Update system
      pacman:
        update_cache: yes
        upgrade: yes

    - name: archlinux-keyring - enable weekly update
      systemd:
        name: archlinux-keyring-wkd-sync.timer
        enabled: yes
        state: started

    - name: Install essential packages
      pacman:
        name: "{{ base_packages }}"
        state: present

    - name: makepkg - enable parallel compilation"
      lineinfile:
        path: /etc/makepkg.conf
        regexp: '#MAKEFLAGS="-j2"'
        line: 'MAKEFLAGS="-j$(nproc)"'

    - name: enable weekly SSD disks trim
      systemd:
        name: fstrim.timer
        enabled: yes
        state: started

    - name: Start and enable SSH service
      systemd:
        name: sshd
        state: started
        enabled: yes

    - name: Configure SSH hardening
      lineinfile:
        path: /etc/ssh/sshd_config
        line: "{{ item }}"
        state: present
      loop:
        - "PermitRootLogin no"
        - "PasswordAuthentication no"
      notify: restart sshd

    - name: Allow SSH through UFW
      ufw:
        rule: allow
        port: '22'
        proto: tcp

    - name: Allow HTTP and HTTPS through UFW
      ufw:
        rule: allow
        port: "{{ item }}"
        proto: tcp
      loop:
        - '80'
        - '443'

    - name: Ensure UFW is enabled
      ufw:
        state: enabled
        policy: deny
        direction: incoming

    - name: Configure fail2ban
      copy:
        content: |
          [DEFAULT]
          bantime = 3600
          findtime = 600
          maxretry = 3

          [sshd]
          enabled = true
          port = ssh
          logpath = /var/log/auth.log
          maxretry = 3
        dest: /etc/fail2ban/jail.local
        mode: '0644'

    - name: Start and enable fail2ban
      systemd:
        name: fail2ban
        state: started
        enabled: yes

  handlers:
    - name: restart sshd
      systemd:
        name: sshd
        state: restarted
